services:
  postgres:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_DB: nihon_agent
      POSTGRES_USER: nihon
      POSTGRES_PASSWORD: nihon
    ports: ["5432:5432"]
    volumes: ["pgdata:/var/lib/postgresql/data"]
volumes:
  pgdata:
